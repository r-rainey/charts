apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "duckdns.fullname" . }}-test-connection"
  labels:
    {{- include "duckdns.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: ls
      image: busybox
      command: ['ls']
  restartPolicy: Never
